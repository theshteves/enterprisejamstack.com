<h1>
  Confirmed<span className="text-brand-yellow font-funky text-5xl">!</span>
</h1>

You'll receive emails from me (Jason Lengstorf) about Jamstack.

Every email I send about Jamstack with Jason course will have a handy unsubscribe link at the bottom. Please feel free to use it if you don't want to receive emails about this!

Thanks,<br />
Jason

import Layout from 'layouts'
import Background from 'components/hero/background'

export default ({children}) => (
  <Layout noFooter>
    <div className="min-h-screen w-full flex items-center justify-center relative py-40 px-5">
      <div className="max-w-md relative z-10 prose">{children}</div>
      <Background />
    </div>
  </Layout>
)
